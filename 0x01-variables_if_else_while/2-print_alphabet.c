#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 * Description:  prints the alphabet in lowercase, followed by a new line
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar('\n');
	}
	return (0);
}
