#include "main.h"

/**
 * int_strlen - a function that returns the length of a  string
 *
 * @s: string input
 *
 * Return: Always 0 (Success)
 */

int _strlen(char *s)
{
	int len;
	s = "";

	putchar("%c", s);
	return (0);
}
