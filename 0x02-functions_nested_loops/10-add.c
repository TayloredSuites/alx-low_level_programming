#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @i: integer being added
 * @j: integer being added
 * Return: result of addition
 */

int add(int i, int j)
{
	int result;

	result = i + j;
	return (result);
}
