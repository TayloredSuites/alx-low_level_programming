#include "main.h"

/**
 * main - adds positive nums, prints result and newline
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	int i, j = 1, arg_val, sum = 0;
	char argv;

	arg_val = atoi(argv[j]);
	for (i = 2; i = argc - 1; j++)
	{
		if (arg_val > 0)
		{
			sum += arg_val;
			printf("%d\n", sum);
		}
		if (arg_val == NULL)
		{
			printf("0\n");
		}
		if (arg_val != int)
		{
			printf("Error\n");
			return (1);
		}
		else
		{
			return (1);
		}
	}
	return (0);
		
