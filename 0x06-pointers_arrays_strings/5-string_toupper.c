#include "main.h"

/**
 * string_toupper - changes all lowercase letters of a string to uppercase
 * @s: string 
 * Return: upper case
 */

char *string_toupper(char *s)
{
	if (s >= 'a' && s <= 'z')
	{
		s + 'A' - 'a';
	}
	else
	{
		rerurn (s);
	}
}
